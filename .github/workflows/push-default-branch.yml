name: 'Default branch'

on:
  push:
    branches:
      - 'main'

jobs:
  build:
    name: 'Build'
    uses: ./.github/workflows/build.yml
  deploy:
    name: 'Deploy'
    uses: ./.github/workflows/deploy.yml
    needs:
      - build
